<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FamilyTree"
        tools:targetApi="31">
        <activity
            android:name=".AddTreeActions.EmptyTreeFirstPersonCreate"
            android:exported="false"
            android:label="@string/title_activity_empty_tree_first_person_create"
            android:theme="@style/Theme.FamilyTree" />
        <activity
            android:name=".AddTreeActions.TreesViewActivity"
            android:exported="false"
            android:label="@string/title_activity_trees_view"
            android:theme="@style/Theme.FamilyTree" />
        <activity
            android:name=".AddTreeActions.AddTreeActivity"
            android:exported="false"
            android:label="@string/title_activity_add_tree"
            android:theme="@style/Theme.FamilyTree" />
        <activity
            android:name=".MainPage"
            android:exported="false"
            android:label="@string/title_activity_main_page"
            android:theme="@style/Theme.FamilyTree" />
        <activity
            android:name=".LogInActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true"
            android:label="@string/title_activity_log_in"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.FamilyTree">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegistrationActivity"
            android:exported="false"
            android:label="@string/title_activity_registration"
            android:theme="@style/Theme.FamilyTree" />
    </application>

</manifest>